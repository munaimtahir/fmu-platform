
    ### Student (MVP)
    - id: Auto (PK)
    - reg_no: string (unique)
    - name: string
    - program: string
    - status: string

    Note: "read-own" derives from auth_user.username == student.reg_no (MVP).
    
