# Google Play internal testing

This directory contains only public release material. `upload_certificate.pem` is the public upload certificate; the private upload key is outside Git. Upload the signed AAB to Google Play Internal Testing, then use Play App Signing.
